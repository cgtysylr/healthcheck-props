apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcheck-controller
  labels:
    app: healthcheck-controller
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: healthcheck-controller
  template:
    metadata:
      labels:
        app: healthcheck-controller
        version: v1
    spec:
      containers:
      - name: healthcheck-controller
        image: healthcheck-controller:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: FLASK_ENV
          valueFrom:
            configMapKeyRef:
              name: healthcheck-controller-config
              key: flask_env
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
          successThreshold: 1
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true

